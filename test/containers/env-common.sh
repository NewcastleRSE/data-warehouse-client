#!/bin/bash

DEBUG=

###############################################################################
# Trim leading and trailing whitespace
###############################################################################
trim () {
    var=$1
    var="${var#"${var%%[![:space:]]*}"}"
    var="${var%"${var##*[![:space:]]}"}"
    echo $var
}
